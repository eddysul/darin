# 다린 앱 UI/UX 감사 보고서

- 작성일: 2026-08-19
- 대상: 현재 워킹 트리 (`main` + 미커밋 변경분)
- 성격: 읽기 전용 감사로 작성. **이후 1차 항목에 한해 수정 적용 (§0.4 참조).**
- 방법: 화면 4개 영역 병렬 정적 감사 + 테마 토큰 대비 실측 + 횡단 지표 계측
- 참고: 기능·데이터 정합성 이슈는 별도 문서 `QA_감사_보고서_20260819.md` 참조. 본 문서는 UI/UX만 다룹니다.

---

## 0. 요약

### 0.1 발견 건수

| 심각도 | 건수 | 성격 |
|---|---|---|
| High | 26 | 사용자가 작업을 완료하지 못하거나, 잘못된 정보를 안내받거나, 되돌릴 수 없는 손실이 발생 |
| Medium | 62 | 작업은 가능하나 마찰이 크거나 일관성이 깨짐 |
| Low | 32 | 다듬기 수준 |
| 합계 | **약 120** | |

### 0.2 영역별 분포

| 영역 | High | 주요 성격 |
|---|---|---|
| 인증·온보딩·프로필 설정 | 7 | 로딩 부재, 필수 항목 안내 부재, 동의 미저장 |
| 기록 홈 (기록 탭) | 6 | 라벨과 동작 불일치, 삭제 확인 부재, 키보드 가림 |
| 일기·추억·리포트·상담 | 8 | 업로드 상태 미노출, 미저장 변경 손실, AI 응답 피드백 |
| 내비게이션·설정·가족공유·알림 | 5 | 탭 IA, 파라미터 잔존, 잘못된 비활성 사유 |
| 횡단 (색·타이포·i18n·접근성) | — | 색 대비 미달, 토큰 미사용, 접근성 커버리지 |

### 0.3 재확인 과정에서 정정된 지적

| 지적 | 판정 | 근거 |
|---|---|---|
| 알림센터가 프로덕션에서 QA 더미 알림을 노출 | **재현 불가 / 이미 차단됨** | `src/data/notificationQaSeed.ts:6` — `ENABLE_NOTIFICATION_QA_SEED = __DEV__`. 릴리스 빌드에서 `getNotificationQaSeed()`는 빈 배열 반환 |
| 기록 생성 토스트가 탭 바를 가림 | **미확인** | `RecordCreatedToast`는 탭 씬 내부에 렌더링되고 커스텀 탭 바는 씬의 형제 요소라 `bottom: 18`은 탭 바 위 18pt. 다만 **CTA 문구 대비 미달**은 별건으로 확인됨 (§4.1) |

이 두 건은 대응 대상에서 제외해도 됩니다.

### 0.4 1차 수정 적용 현황 (2026-08-19)

| 항목 | 상태 | 수정 내용 |
|---|---|---|
| 1-1 마케팅 동의 폐기 | 적용 | `termsStore`에 동의 여부와 결정 시각을 저장하고, 설정 › 수유/수면 알림에서 철회·재동의 가능 |
| 1-2 기록 삭제 확인 부재 | 적용 | 타임라인 스와이프와 동일한 `Alert` 확인 적용 |
| 1-3 마이크 비활성 사유 오안내 | **정책 변경으로 해소** | 임신 중 마이크 차단을 철회. 아래 0.5 참조 |
| 1-5 `convertBirth` 파라미터 잔존 | **재현 불가** | React Navigation v7의 `navigate`는 `merge`가 opt-in (`StackRouter.tsx:400-412`). 기존 호출은 파라미터를 교체하므로 잔존하지 않음 |
| 일기 편집 미저장 손실 (§2.3) | 적용 | 편집 모드에서 변경분이 있으면 닫기 시 확인 |
| 추억 업로드 중 제출 (§2.3) | 적용 | 업로드 미완료 시 "올리기" 차단 + 썸네일 진행 표시 |
| 알림센터 실패와 빈 상태 혼동 (§2.4) | 적용 | 조회 실패 시 재시도 버튼이 있는 오류 상태를 별도 표시 |

### 0.5 마이크 정책 변경 (2026-08-19)

QA 감사의 H2("임신 중 음성으로 출생 후 카테고리 기록 생성")는 최초에 **임신 중 마이크 차단**으로 막았습니다. 이후 "임신 중에도 음성 기록은 쓸 수 있어야 한다"는 판단에 따라 차단을 걷어내고, 대신 **매핑 단계에서 임신 카테고리로만 해석**하도록 바꿨습니다.

| 변경 | 위치 |
|---|---|
| 임신 중 임신 전용 어휘 파서 추가 (`pregMood`·`pregKick`·`pregSymptom`·`pregHospital`·`pregMed`·`pregWeight`·`pregBp`) | `src/utils/voiceToBabyLog.ts` |
| 임신 중에는 결과를 임신 카테고리로만 필터링, 미매칭 발화는 `memo` 대신 `pregMood`로 폴백 | `src/utils/voiceToBabyLog.ts` |
| 마이크 차단 조건에서 임신 상태 제거 (쓰기 권한 없음만 잠금) | `src/screens/MainTabs.tsx` |
| 확정·수동 입력·상세 저장 경로에 임신 카테고리 가드 | `src/screens/MainTabs.tsx` |

결과적으로 H2가 노린 데이터 정합성(임신 중 출생 후 기록 생성 금지)은 유지되면서, 기능 손실은 없어졌습니다. 본문 §1-3, §2.2의 "마이크 비활성" 서술은 이 정책으로 대체됩니다.

### 0.6 2차 수정 적용 현황 (2026-08-19)

| 항목 | 상태 | 수정 내용 |
|---|---|---|
| 8 `faint`/`muted` 대비 | 적용 | `faint` `#A39E96`→`#736D65`, `muted` `#7A746C`→`#6B655E`. 가장 밝은 표면(`cardHi` `#FAF4EE`)에서도 4.5:1 이상 |
| 8 토스트 CTA 대비 | 적용 | `accentOnDark` `#F7B3AB` 토큰 신설, 두 토스트에 적용 (2.7:1 → 약 9:1) |
| 8 주요 버튼 대비 | 적용 | 코랄 채움은 유지하고 라벨을 잉크색으로. 2.37:1 → 6.0:1. 아래 0.7 참조 |
| 9 키보드 회피 | 적용 | `ActiveTimerSheet`(+`ScrollView`), `AddCustomCategorySheet`, `GrowthRecordModal`에 `KeyboardAvoidingView`. 모달 바텀시트 5곳의 안드로이드 `behavior`를 `undefined`→`height` |
| 10 필수 항목 미충족 사유 | 적용 | `ProfileSetupScreen`: 보호자 생년월일에 `*` 추가, 닉네임 인라인 오류, CTA 위 "남은 항목" 안내 + `accessibilityHint` |
| 11 사진 대체 텍스트 | 적용 | 추억 피드 카드와 상세 뷰어 이미지에 캡션·장수 기반 레이블 |
| 11 기록 생성 live region | 적용 | `RecordCreatedToast`에 `accessibilityLiveRegion` + iOS용 `announceForAccessibility`, 자동 닫힘 3.2s→5s |
| 12 터치 타깃 | 부분 적용 | `QuickRecordEditorSheet` 고정/수정/삭제 버튼 약 31pt → 44pt + 레이블 |
| (추가) 흰색 텍스트가 흰 배경에 놓인 결함 6곳 | 적용 | 아래 0.7 참조 |

추가로 `PushToast`가 노치 아래로 들어가던 문제(`top: 14` 고정)를 safe area 기준으로 바꾸고 닫기 버튼을 넣었습니다.

### 0.7 `amberDark` 토큰 과부하 — 신규 발견

`colors.amberDark`는 이름과 달리 값이 `#FFFFFF`이고, 주석은 "코랄 채움 위의 텍스트"라고 되어 있지만 실제로는 세 가지 배경에 쓰이고 있습니다.

1. 코랄 채움 위 (의도된 용법, 다만 2.37:1로 AA 미달)
2. 어두운 스크림 위 — 예: `MemoryEditModal` 사진 삭제 버튼(`rgba(46,42,38,0.72)`). 여기서는 흰색이 맞습니다
3. **밝은 배경 위 — 글자가 보이지 않음**

3번은 실제 결함이라 이번에 고쳤습니다.

| 위치 | 증상 |
|---|---|
| `BabyLogVoiceOverlay` `warnBanner`, `understoodAs` | 흰 카드 위 흰 글씨 |
| `BabyLogVoiceOverlay` `timeChoiceText` | `amberSoft`(연분홍) 위 흰 글씨 |
| `GrowthBookPageCanvas` `addPhotoLabel`, `emptyRolling` | 반투명 흰 배경 위 흰 글씨 |
| `GrowthBookPageCanvas` `photoMoveBadge` | 배지 배경을 흰색으로 덮어써 흰 글씨가 사라짐 |

1번(주요 버튼 라벨)은 **A안 — 코랄 채움을 그대로 두고 라벨을 잉크색으로** 결정해 적용했습니다. 팔레트는 유지되고 대비는 2.37:1 → 6.0:1이 됩니다.

토큰은 두 개로 분리했습니다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `amberDark` (`primaryForeground` 별칭) | `#2E2A26` | 코랄 채움 위 콘텐츠 |
| `onDark` | `#FFFFFF` | 어두운 채움·스크림 위 콘텐츠 |
| `accentOnDark` | `#F7B3AB` | 어두운 표면 위 강조 텍스트 (토스트) |

전체 사용처를 배경별로 전수 분류해 적용했습니다.

| 분류 | 건수 | 처리 |
|---|---|---|
| 코랄 채움 위 | 53 | `amberDark` 값 변경으로 자동 반영 |
| 어두운 배경 위 | 4 | `onDark`로 전환 — 사진 삭제 버튼 3곳(`rgba(46,42,38,0.72)`), Apple 로그인 버튼(`#2E2A26`) |
| 밝은 배경 위 (기존 결함) | 6 | `amberText`로 전환 |

토큰을 쓰지 않고 흰색을 하드코딩한 곳도 함께 훑었습니다. 코랄 채움 위 18곳(설정 저장 버튼, 추억 필터 칩, 프로필 저장, 약관 체크마크, 날짜 선택 등)과 대응하는 `ActivityIndicator` 5곳, 중앙 마이크 버튼 아이콘을 잉크색으로 바꿨습니다. 사진 위 배지·삭제 스와이프·토스트 제목처럼 실제로 어두운 배경 위인 곳은 흰색으로 두었습니다.

검증: `tsc --noEmit` 통과, 편집 파일 린트 무결.

---

## 1. 출시 전 반드시 (High, 코드 재확인 완료)

### 1-1. 마케팅 수신 동의가 저장되지 않고 폐기됨

- 위치: `App.tsx:616-621`, `src/screens/onboarding/TermsConsentScreen.tsx:44-48`
- 현상: 온보딩 약관 화면에서 "마케팅 알림 수신에 동의합니다. (선택)"을 체크해도 `handleTermsAccept`가 `void marketingOptIn;`으로 값을 버립니다. 필수 3항목만 `saveTermsAccepted(true)`로 저장됩니다.
- 영향: 동의를 UI로 수집하고 기록하지 않는 구조. UX보다 **컴플라이언스 리스크**가 큽니다. 반대로 사용자가 동의하지 않아도 이후 마케팅 발송을 막을 근거 데이터가 없습니다.
- 방향: 프로필/설정 스토어에 동의 여부와 시각을 저장하거나, 저장할 곳이 없다면 체크박스를 제거.

### 1-2. 기록 삭제에 확인 절차와 실행취소가 없음

- 위치: `src/components/babylog/RecordDetailSheet.tsx:1214-1218`
- 현상: "이 기록 삭제하기"를 누르면 즉시 `onDelete` 호출. 동일 행위인 타임라인 스와이프 삭제(`src/components/babylog/TodayTimeline.tsx:159-174`)는 확인 다이얼로그가 있어 **경로별로 안전장치가 다릅니다.**
- 영향: 한 손 조작 빈도가 높은 앱에서 오탭 시 복구 불가.
- 방향: 타임라인과 동일한 `Alert.alert` 확인을 적용하거나, 삭제 후 실행취소 토스트 제공.

### 1-3. 마이크 비활성화 사유가 사실과 다르게 안내됨

- 위치: `src/screens/MainTabs.tsx:74-77, 130-135, 428`
- 현상: `allowVoice = allowAdd && !isPregnancyStage(careSetup.child)` 인데 힌트는 조건과 무관하게 항상 `"출산 후에 사용할 수 있어요"`. 쓰기 권한이 없는 뷰어·돌보미에게도 같은 문구가 안내됩니다.
- 부수 문제: 시각 처리는 `disabled: { opacity: 0.45 }` 뿐이라 그라디언트가 그대로 보여 활성 버튼처럼 인식되고, 눌러도 아무 피드백이 없습니다. 힌트는 `accessibilityHint`라서 스크린리더 사용자에게만 전달됩니다.
- 영향: 임신 중 사용자에게는 탭 바 중앙(시각적으로 가장 강조된 요소)이 죽어 있는 상태이고, 뷰어에게는 틀린 사유가 전달됩니다.
- 방향: 사유별 분기(임신 / 권한 없음 / 미지원), 시각적으로 명확한 잠금 표현, 탭 시 사유 토스트.

### 1-4. 상담 탭이 탭 바에 없어 위치 감각을 잃음

- 위치: `src/screens/MainTabs.tsx:109-115` (표시 배열), `:369` (라우트 등록)
- 현상: `Consult`는 `Tab.Screen`으로 등록되어 있으나 `tabs` 배열에 없습니다. 진입 시 어떤 탭도 활성 표시되지 않습니다.
- 영향: AI 상담이라는 핵심 기능의 발견 가능성이 낮고, 진입 후 현재 위치와 복귀 경로가 불명확합니다.
- 방향: 탭 노출 또는 모든 화면에 일관된 진입점(헤더 액션) 제공.

### 1-5. 출생 등록 모드 파라미터가 다른 탭에 잔존 — **정정: 재현 불가**

- 위치: `src/screens/MainTabs.tsx:274-279` vs `:295, :311, :328`
- 최초 지적: 기록 탭만 `merge: false`로 `BabyProfile`을 열고 나머지 탭은 기본 merge라, 출생 전환 직후 다른 탭에서 프로필을 열면 `mode=convertBirth`가 남는다.
- 재확인 결과: React Navigation v7에서 `navigate`의 파라미터 병합은 **opt-in**입니다. `@react-navigation/routers@7.6.0`의 `StackRouter.tsx:400-412`는 `action.payload.merge`가 참일 때만 기존 `route.params`를 펼치고, 그 외에는 `createParamsFromAction`으로 파라미터를 교체합니다. 따라서 `navigate("BabyProfile", { mode: undefined })`는 이전 `mode`를 지웁니다. 기록 탭의 `merge: false`는 기본값을 명시한 것뿐입니다.
- 판정: 수정 불필요. 호출부 표기 불일치는 동작에 영향이 없습니다.

### 1-6. 색 대비가 앱 전반에서 WCAG AA 미달

- 위치: `src/theme.ts`
- 실측 (상대 휘도 기반 계산):

| 조합 | 실측 대비 | AA 기준 | 사용 규모 | 상태 |
|---|---|---|---|---|
| `faint` `#A39E96` on `#FFFFFF` | **2.5:1** | 4.5:1 | 본문/설명 텍스트 약 145곳 | 적용 (`#736D65`) |
| `muted` `#7A746C` on `#FEF7F2` | **4.36:1** | 4.5:1 | 보조 텍스트 | 적용 (`#6B655E`) |
| `amberText` `#B03A34` on `rgba(30,32,42,0.96)` | **2.7:1** | 4.5:1 | 두 토스트의 액션 문구 | 적용 (`accentOnDark`) |
| `#FFFFFF` on `amber` `#E8918A` | **2.37:1** | 4.5:1 | 모든 주요 버튼 라벨 (`primaryForeground`, `amberDark`) | 적용 (`#2E2A26`, 6.0:1) |

- 영향: `colors.faint`는 "설명을 읽어야 하는" 자리에 집중되어 있습니다 — `BabyReportScreen` 29곳, `DiaryComposeModal` 8곳, 모든 빈 상태 안내문(`src/components/states/FeedbackStates.tsx:107`), 대부분의 입력 placeholder. 즉 **도움이 가장 필요한 순간의 문구가 가장 안 읽힙니다.** 야외·수유등 아래 사용이 잦은 육아 앱에서 체감이 큽니다.
- 토스트 CTA 미달은 "탭해서 수정 →"이라는 유일한 즉시 수정 진입점을 가립니다 (`RecordCreatedToast.tsx:48`, `PushToast.tsx:62`).
- 방향: `faint`/`muted` 토큰 명도 하향, 주요 버튼은 배경을 진하게 하거나 라벨을 `amberText` 계열로 전환, 어두운 표면 전용 accent 토큰 신설.

### 1-7. 첫 로딩 구간에 아무 피드백이 없음

- 위치: `src/screens/tabs/RecordScreen.tsx:684, 701-702`, `App.tsx:593-599` + `src/screens/SplashScreen.tsx:18-21`
- 현상: `!storageReady`인 동안 타임라인에 빈 배열과 `listEmpty={null}`이 전달되어 스피너 없이 공백이 보입니다. 스플래시 2.6초 이후 `routeAuthenticatedSession()`이 도는 동안에도 정적 로고만 유지됩니다.
- 방향: 스켈레톤 또는 `LoadingState` 적용.

---

## 2. 영역별 상세

### 2.1 인증 · 온보딩 · 프로필 설정

#### 로딩 / 오류 / 빈 상태

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `AuthStartScreen.tsx:45,72,97` | 소셜 로그인이 `null` 반환(사용자 취소, 브라우저 닫힘) 시 아무 메시지 없이 스피너만 멈춤 | `!result`일 때 `socialError`에 취소 안내 설정 |
| Medium | `OnboardingFlow.tsx:168-187,454-461` | "코드 확인"에 로딩 상태가 없어 네트워크 중 반복 탭 가능 | `inviteBusy` + 버튼 비활성 + 스피너 |
| Medium | `OnboardingFlow.tsx:492-505` | "초대 수락"이 비동기인데 진행 표시 없음 | 동일 |
| Medium | `OnboardingShell.tsx:109-118` | 셸 주 버튼에 busy 상태 슬롯 자체가 없음 | `primaryBusy` prop 추가 |
| Medium | `ProfileSetupScreen.tsx:96,307-311` | `nicknameError`가 제출을 막지만 화면에 렌더링되지 않음 | 필드 하단 인라인 오류 표시 |
| Medium | `FeedbackStates.tsx` 전체 | 공용 상태 컴포넌트가 이 퍼널에서 전혀 사용되지 않고 화면마다 자체 패턴 | 온보딩 전 구간 표준화 |
| Low | `EmailAuthForm.tsx:141-142` | 매핑되지 않은 Supabase 오류가 영어 원문으로 노출 | 한국어 일반 fallback |

#### 폼 UX

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `ProfileSetupScreen.tsx:292-304, 98-105` | "보호자 생년월일"에 `*` 표시가 없는데 필수. 제출 버튼만 계속 비활성 | 라벨에 `*` + 미입력 시 helper |
| High | `ProfileSetupScreen.tsx:219-230, 307-311` | 닉네임 규칙 위반(길이, `#`, `/`) 시 안내 없이 버튼만 45% 투명 | 입력 중 인라인 검증 메시지 |
| Medium | `EmailAuthForm.tsx:80-81, 98-105` | 이메일 검증이 `includes("@")` 뿐이라 제출 후에야 실패 | blur 시 형식 검증 |
| Medium | `EmailAuthForm.tsx:193-224` | `autoComplete`, `returnKeyType`, `onSubmitEditing` 없음 → 자동완성·키보드 이동 불편 | 표준 속성 추가 |
| Medium | `EmailAuthForm.tsx:249-251` | 빈 필드에도 제출 가능, 실패 시 상단 일반 오류만 표시 | 필드별 검증으로 전환 |
| Medium | `EmailAuthForm.tsx:286-290` | `reset-password` 모드에만 "로그인으로 돌아가기" 링크 없음 | 이탈 경로 추가 |
| Medium | `ProfileSetupScreen.tsx:227-228` | `maxLength={12}` 무음 절단, 카운터 없음 | 글자 수 표시 |
| Medium | `OnboardingFlow.tsx:469-474` | 초대 코드 입력에서 잘못된 문자를 조용히 제거 | 거부 사유 힌트 |
| Low | `EmailAuthForm.tsx:202` | placeholder가 영어(`you@example.com`) | 한국어 예시 |
| Low | `OnboardingShell.tsx:61-64`, `ProfileSetupScreen.tsx:192-195` | `keyboardVerticalOffset={0}`, Android `behavior={undefined}`로 하단 CTA 가림 가능 | 오프셋 조정 |

#### 진행 상태 / 방향 감각

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `ProfileSetupScreen.tsx:191-331` | 단계 표시·뒤로가기·로그아웃이 모두 없는 전체 화면. 퍼널 내 위치와 이탈 경로를 알 수 없음 | `OnboardingShell`로 감싸고 단계 pill + 보조 이탈 링크 |
| Medium | `OnboardingFlow.tsx:156-161, 345-380` | `connect`, `requests`, `invite`, `invite-confirm`, `complete`에서 `progressStep`이 `undefined` | 진행 모델 확장 |
| Medium | `App.tsx:731`, `OnboardingFlow.tsx:92` | 프로덕션은 `skipProfileStep`이라 첫 표시가 **2/3**부터 시작 | 경로별 단계 재번호 |
| Medium | `OnboardingFlow.tsx:570` | 임신 경로는 `care`를 건너뛰어 3단계를 암시하고 2단계만 존재 | 임신 경로 2단계 트랙 |
| Medium | `TermsConsentScreen.tsx:19-24` | 약관·인증 단계에는 진행 표시가 전혀 없음 | 퍼널 전체 진행 표시 |
| Low | `EmailAuthForm.tsx:180-181, 207-210` | "로그인으로 계속하기" 라벨과 실제 동작(`completePendingEmailAuth`)이 불일치 | 라벨 변경 또는 액션 분리 |

#### 비활성 상태의 사유 부재

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `ProfileSetupScreen.tsx:307-314, 363-364` | 관계·국가·언어·생년월일 중 무엇이 비었는지 알려주지 않고 "다음"만 45% 투명 | CTA 근처 체크리스트 또는 안내 |
| High | `TermsConsentScreen.tsx:23`, `OnboardingShell.tsx:290` | 필수 3항목 중 남은 것을 알려주지 않음 | "필수 3항목에 모두 동의해 주세요" |
| Medium | `AuthStartScreen.tsx:147, 216-217, 249` | Android에서 Apple 버튼이 영구 비활성 상태로 노출 | Android에서 숨기기 |
| Medium | `AuthStartScreen.tsx:216-217` | 프로바이더 플래그 off 시 58% 투명 버튼만 남아 고장처럼 보임 | 비활성 사유 캡션 |
| Medium | `OnboardingFlow.tsx:569` | 아기 단계에서 이름/날짜 중 무엇이 비었는지 미표시 | 빈 필드 강조 |

#### 터치 타깃 (44pt / 48dp 미만)

| 심각도 | 위치 | 실측 | 개선 방향 |
|---|---|---|---|
| Medium | `OnboardingShell.tsx:171-177, 265-272` | 옵션 행 ~33pt | `minHeight: 44/48` |
| Medium | `OnboardingFlow.tsx:712-718, 839-846` | 케어 설정 칩 ~31pt | 동일 |
| Medium | `ProfileSetupScreen.tsx:353` | 칩 `minHeight: 42` (iOS 2pt, Android 6dp 부족) | `Platform.select` |
| Medium | `OnboardingShell.tsx:292-293` | "뒤로" 링크 ~30pt | `minHeight: 44` |
| Medium | `EmailAuthForm.tsx:274-281, 346` | 비밀번호 찾기 / 회원가입 링크 | 패딩·hitSlop 확대 |
| Medium | `AuthStartScreen.tsx:178-184` | 약관 링크 11px + `hitSlop={10}` | 44pt 래퍼 |

#### 한국어 카피 일관성

| 심각도 | 위치 | 문제 |
|---|---|---|
| High | `TermsConsentScreen.tsx:49` | 로그인 전인데 "약관 전문은 **메뉴 › 개인정보/약관**에서 확인" — 도달 불가 경로 안내. 고지 기반 동의가 성립하지 않음 |
| Medium | `ProfileSetupScreen.tsx:243` vs `OnboardingFlow.tsx:308` | "**아기**와의 관계" vs "**아이**와의 관계" |
| Medium | `ProfileSetupScreen.tsx:219` vs `OnboardingFlow.tsx:298,574` | "**닉네임**" vs "**이름**" |
| Medium | `OnboardingFlow.tsx:574,585,755` | "아기 이름/별명" / "태명/별명" / "아기 이름" / "태명" 혼용 |
| Low | `AuthStartScreen.tsx:59,105` 등 | "다시 시도**해주세요**" vs "다시 시도**해 주세요**" |
| Low | `OnboardingFlow.tsx:543` | "**뱃속**에 있어요" vs 다른 화면의 "**임신 중**" |

### 2.2 기록 홈 (기록 탭)

#### 주요 작업 효율 — 탭 수 기준

| 작업 | 경로 | 최소 탭 |
|---|---|---|
| 고정된 자주 쓰는 기록 (분유 120ml 등) | QuickRecordsBar 칩 | **1** |
| 임신 자주 쓰는 기록 | QuickRecordsBar 칩 | **1** |
| 그리드 카테고리 (분유, 기저귀 등) | 그리드 → 상세 시트 → 저장 | **2–4+** |
| 타이머 시작 | 그리드 타일 롱프레스 | 롱프레스 1 + 종료 흐름 |
| 진행 중 수면 종료 | 그리드 → 타이머 시트 | **2+** (라벨은 1탭을 암시) |

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `OneTouchRecordGrid.tsx:336-337`, `RecordScreen.tsx:309-314` | 수면 진행 중 타일이 "**수면 종료**"인데 탭하면 종료가 아니라 타이머 시트가 열림 — 라벨과 동작 불일치 | 1탭 종료로 맞추거나 "수면 관리"로 개명 |
| High | `RecordScreen.tsx:392-396` | 그리드 탭은 항상 상세 시트. 분유·기저귀 같은 최빈 항목도 1탭 저장 경로 없음 | 핵심 카테고리 기본값 즉시 기록 |
| Medium | `OneTouchRecordGrid.tsx:78-82` | 임신 모드 안내문에 롱프레스 설명이 빠짐. 자주 쓰는 기록의 "롱프레스로 수정"은 어디에도 안내 없음 | 힌트 문구 또는 최초 1회 코치마크 |
| Medium | `QuickRecordsBar.tsx:106-113` | 칩 롱프레스로 편집 진입하지만 시각적 단서 없음 | 편집 어포던스 추가 |
| Medium | `OneTouchRecordGrid.tsx:66-70,155-164` | 접힌 그리드가 상위 일부만 노출, 나머지는 "더 보기" 후 2탭 | "N개 더" 배지 명시 |
| Medium | `RecordScreen.tsx:563-570` | 프리셋 칩이 없는 기저귀 자주 쓰는 기록은 저장이 아니라 전체 시트가 열림 | 설정 시 칩 필수화 또는 최근값 기본 적용 |
| Low | `RecordScreen.tsx:552-558` | 1.2초 내 재탭이 조용히 무시됨 (중복 방지 가드) | "방금 기록했어요" 토스트 |
| Low | `RecordScreen.tsx:765`, `OneTouchRecordGrid.tsx:391` | 뷰어 계정은 비활성 컨트롤만 보고 탭해도 무반응 | 보기 전용 사유 안내 |

#### 피드백 / 확인

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| Medium | `RecordScreen.tsx:362-371, 825-836` | 상세 시트 저장에는 토스트가 없고, 자주 쓰는 기록·타이머 종료에만 `announceCreated` 호출 | 저장 경로 전체에 확인 통일 |
| Medium | `RecordCreatedToast.tsx:13-18` | 실행취소 없음. 3.2초 안에 "탭해서 수정"만 가능 | "실행 취소" 액션 추가 |
| Medium | `RecordCreatedToast.tsx:14-17` | 3.2초 자동 소멸, 진행 표시 없음 | 5초 내외로 연장 |
| Low | `RecordScreen.tsx:671-679` | 기록 생성 시 햅틱 없음 | 가벼운 햅틱 |

#### 파괴적 동작

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `RecordDetailSheet.tsx:1214-1217` | 확인 없는 즉시 삭제 (§1-2) | 확인 다이얼로그 |
| Medium | `QuickRecordEditorSheet.tsx:271-273` | 사용자 정의 자주 쓰는 기록 "삭제"가 즉시 실행 | 확인 또는 실행취소 |
| Medium | `TodayTimeline.tsx:159-174` | 확인은 있으나 삭제 후 실행취소 없음 | 실행취소 토스트 |
| Low | `RecordDetailSheet.tsx:672-674` | 배경 탭으로 닫으면 작성 중 내용이 경고 없이 사라짐 | dirty 상태 확인 |
| Low | `OneTouchRecordGrid.tsx:315-322` | `delayLongPress={380}` — 조밀한 타일에서 지연 탭처럼 느껴질 수 있음 | 450–500ms 검토 |

#### 바텀시트

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `ActiveTimerSheet.tsx:122-198` | `KeyboardAvoidingView`·`ScrollView` 모두 없음. 수유량 입력 시 "종료 및 저장"에 손이 닿지 않을 수 있음 | KAV + 스크롤 |
| Medium | `AddCustomCategorySheet.tsx:106-234` | `autoFocus` 입력 + 고정 푸터, 키보드 회피 없음 | 동일 |
| Medium | `QuickRecordEditorSheet.tsx:205-207` | `behavior`가 iOS 전용이라 Android 미대응 | 크로스 플랫폼 처리 |
| Medium | `RecordDetailSheet.tsx:689-1228` | 긴 스크롤 하단에 저장/취소. 스티키 푸터 없음 | 하단 고정 액션 바 |
| Medium | `BottomSheet.tsx:13-38` | 공용 시트 래퍼가 있는데 기록 흐름은 개별 Modal을 사용 — 패딩·radius·최대 높이 불일치 | 공용 래퍼로 통합 |
| Low | `RecordDetailSheet.tsx:677` | 핸들 바가 장식용이고 실제 닫기는 배경 탭뿐 | 명시적 "닫기" |
| Low | `RecordDetailSheet.tsx:218-345` | 열릴 때 첫 필수 입력에 포커스하지 않음 | 자동 포커스 |

#### 터치 타깃 / 접근성 / 모션 (기록 탭)

| 심각도 | 위치 | 문제 | 개선 방향 |
|---|---|---|---|
| High | `RecordCreatedToast.tsx:22-28` | `accessibilityRole`/live region 없음 — 스크린리더가 기록 생성을 인지하지 못함 | `accessibilityLiveRegion="polite"` |
| High | `ActiveTimerSheet.tsx:126-194` | 일시정지·좌우 전환·시계에 role/label/state 없음 | 전면 라벨링 |
| Medium | `OneTouchRecordGrid.tsx:324-327` | 라벨은 있으나 `accessibilityRole="button"` 없음 | role + state 추가 |
| Medium | `AmountInput.tsx:40-68` | 단위 칩에 선택 상태 없음 | `accessibilityState={{ selected }}` |
| Medium | `RecordDetailSheet.tsx:115-124` | 공용 `ChipRow`에 라벨 없음 | 칩 텍스트 전달 |
| Medium | `BabySwitcher.tsx:137-143` | `switchButton` 변형 `minHeight: 0`, 11px | `minHeight: 44` |
| Medium | `TodayTimeline.tsx:318-325` | "수정" 칩 ~28pt | 행 높이 확대 |
| Medium | `QuickRecordEditorSheet.tsx:478-484` | 고정/수정/삭제 미니 버튼 `paddingVertical: 5` | `minHeight: 44` |
| Medium | `OneTouchRecordGrid.tsx:390`, `QuickRecordsBar.tsx:208` | press scale 변환이 reduce-motion 무시 | 설정 반영 |
| Medium | 모든 Modal | `animationType="slide"` 고정 | reduce-motion 시 fade/none |
| Low | `OneTouchRecordGrid.tsx:410-420` | "진행 중" 배지 8px | 10–11pt |
| Low | `RecordHomeHeader.tsx:84,158` | 컴팩트 아바타 40×40 + `hitSlop: 4` | 44pt 확보 |

#### 임신 / 출생 단계 차이

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `MainTabs.tsx:130-135, 428` | 임신 중 마이크 비활성 표현·사유 문제 (§1-3) | 사유별 분기 + 명시적 잠금 |
| Medium | `TodayLogSummaryCard.tsx:129-177` | 임신 요약이 기록 0건이어도 **7개 지표 카드를 0회로 항상 표시**. 출생 모드는 핵심 3개 + 기록된 항목만 표시 | 0건 지표 숨김 또는 상위 3개 |
| Medium | `OneTouchRecordGrid.tsx:77-81` | 안내문은 "탭해서 임신 기록"인데 실제로는 출생 모드와 같은 2탭 이상 흐름 | 카피와 실동작 정렬 |
| Medium | `RecordScreen.tsx:779-782` | 임신 중 성장 기록 진입점이 사라지고 대체 항목 없음 | 산전 검진 바로가기 등 |

### 2.3 일기 · 추억 · 리포트 · 상담

#### 미디어 업로드

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `MemoryUploadModal.tsx:161-183, 220-226` | 압축·업로드가 끝나기 전에 "올리기"를 누를 수 있음. 진행률 표시는 없고 `failed`에만 재시도 오버레이 | 전 작업 완료 전 제출 차단 + 썸네일별 상태 |
| High | `DiaryComposeModal.tsx:252-266, 415-437` | 사진이 eager 업로드에 등록되지만 UI는 정적 썸네일. **백그라운드 업로드 실패가 저장 시점까지 보이지 않음** | `subscribeEagerSession` 구독 후 추억 모달과 동일한 실패/재시도 UI |
| High | `DiaryComposeModal.tsx:286-289, 317` | "일기 수정"에서 닫기·스와이프 시 편집 내용이 경고 없이 사라짐 (추억은 보호됨) | dirty 확인 다이얼로그 |
| Medium | `MemoryEditModal.tsx:89` vs `MemoryUploadModal.tsx:125-158` | 25MB 사전 검사가 편집 모달에만 있음 | 모든 피커에서 `asset.fileSize` 검사 |
| Medium | `DiaryComposeModal.tsx:252-258` | `activeBabyId`가 없으면 썸네일만 추가되고 업로드도 오류도 없음 | 선택 차단 또는 인라인 오류 |
| Medium | `MemoriesScreen.tsx:116-134, 619` | 게시 실패 상태에서는 상세 진입이 막혀 재시도 외 경로가 없음 | 읽기 전용 상세 또는 삭제 옵션 |
| Low | `DiaryComposeModal.tsx:239-241` | 권한 거부 시 설정 딥링크 없음 (`DiaryReminderSettingsModal.tsx:276-278`에는 있음) | "설정 열기" 추가 |
| Low | `MemoryEditModal.tsx:107-113` | 변경이 없어도 취소 시 항상 폐기 경고 | dirty 기반 |

#### 목록 / 긴 콘텐츠

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `ConsultScreen.tsx:318-367` | 전체 대화가 `ScrollView` + `.map` — 세션이 길어지면 끊김·메모리 증가 | inverted `FlatList` |
| Medium | `BabyReportScreen.tsx:168-376` | 리포트 전체가 단일 `ScrollView` | 접힌 블록 지연 마운트 |
| Medium | `MemoriesScreen.tsx:579-635` | `initialNumToRender`·`windowSize`·`removeClippedSubviews` 미설정 | 표준 성능 props |
| Medium | `DiaryScreen.tsx:418-582` | pull-to-refresh 없음 — 네트워크 복구 후 수동 동기화 불가 | `RefreshControl` |
| Medium | `MemoriesScreen.tsx:323-325` | 탭 포커스마다 전체 재조회, 스크롤 위치 미보존 | offset 보존 |
| Low | `MemoryDetailScreen.tsx:348-398` | 댓글이 `ScrollView` + `.map` | `FlatList` |
| Low | `BabyReportScreen.tsx:168` | pull-to-refresh 없음 | 추가 |

#### 모달 / 시트

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `MemoryPrivacyPicker.tsx:10-13` vs `MemoryUploadModal.tsx:163-169` | UI는 3개만 노출하는데 검증은 `tagged_family`/`selected_people`까지 처리. 과거 데이터가 "가족 공개"로 잘못 표시됨 | 옵션 노출 또는 미도달 검증 제거 |
| Medium | `DiaryComposeModal.tsx:596-610` | 작성 Modal 안에 `BabyStickerVaultModal` 중첩 | 단일 호스트로 정리 |
| Medium | `MemoryDetailScreen.tsx:339-447` | 댓글 Modal + 스티커 금고 + 편집 모달 3중 스택 가능 | 순차 처리 |
| Medium | `ConsultMemoSheet.tsx:94` | 배경 탭 시 미저장 메모 경고 없음 | dirty 확인 |
| Medium | `DiaryComposeModal.tsx:349-352` | KAV가 iOS `padding`만 — Android에서 코멘트 필드 가림 가능 | 공통 패턴 |
| Low | `MemoryViewFilterSheet.tsx:39` | 드래그 닫기 제스처 없음 | 팬 제스처 |
| Low | `ConsultPromptSheet.tsx:38-39` | 배경 탭으로 전체 시트가 닫혀 실수 이탈 | 명시적 닫기 |

#### AI 상담 / 음성

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `openaiChat.ts:21-50`, `ConsultScreen.tsx:150-166` | 스트리밍 없음 — 응답 전체가 올 때까지 "AI 분석 중…"만 노출, 느린 망에서 멈춘 것처럼 보임 | 토큰 스트리밍 또는 단계별 상태 |
| Medium | `ConsultScreen.tsx:134-166` | 진행 중 취소 불가 | `AbortController` |
| Medium | `ConsultScreen.tsx:155-159` | 오프라인과 서버 설정 오류가 같은 메시지 | 오류 유형 구분 |
| Medium | `ConsultScreen.tsx:311-315` | AI 서버 미설정 경고 배너가 떠도 입력·전송이 활성 | 전송 비활성 |
| Medium | `ConsultScreen.tsx:325-348` | AI 답변 복사 수단 없음 | 롱프레스 복사 |
| Low | `ConsultScreen.tsx:377-394` | 퀵 칩이 입력만 채우고 전송하지 않아 혼동 | 라벨 명확화 |
| Low | `BabyLogVoiceOverlay.tsx:325-337` | "분석 중"에 취소 경로가 상단 닫기뿐 | 명시적 취소 버튼 |

#### 빈 상태 / 접근성 / 시각 일관성 (해당 탭)

| 심각도 | 위치 | 문제 | 개선 방향 |
|---|---|---|---|
| High | `MemoryMediaViewer.tsx:66-70`, `DiaryComposeModal.tsx:417`, `MemoriesScreen.tsx:102-104` | 사진에 대체 텍스트 전무 | "사진 1/3" 또는 캡션 |
| Medium | `MemoryMediaViewer.tsx:89-92` | 캐러셀 페이지 변경이 안내되지 않음 | 페이지 announce |
| Medium | `MemoryMediaViewer.tsx:43-54` | 더블탭 좋아요에 접근성 대체 수단·힌트 없음 | 명시 버튼 노출 |
| Medium | `DiaryScreen.tsx` 목록 | 최초 로딩 스켈레톤·동기화 오류 배너 없음 — 빈 목록과 구분 불가 | 로딩/오류 상태 |
| Medium | `BabyReportScreen.tsx:147-158` | 임신 빈 상태에 다음 행동 CTA 없음 | 프로필 이동 버튼 |
| Medium | `MemoriesScreen.tsx:696` / `DiaryScreen.tsx:919` / `BabyReportScreen.tsx:942` | 카드 radius 22 / 16 / `radius.lg` 혼재 | 토큰 통일 |
| Medium | `MemoriesScreen.tsx:682` vs `DiaryScreen.tsx:898` | 선택된 필터 칩 텍스트 색이 서로 다름 | 토큰 통일 |
| Medium | `BabyReportScreen.tsx:198, 247, 1011-1012` | `#E9A353`, `#69AFA0`, `#E7F5F0` 등 테마 외 색 | 토큰 매핑 |
| Low | `DiaryComposeModal.tsx:533` | 🌱 이모지를 구조적 아이콘으로 사용 | `BabyLogIcon` |
| Low | `MemoryUploadModal.tsx:296-300` vs `DiaryComposeModal.tsx:618-625` | 모달 헤더 크롬이 서로 다름 | 공용 `ModalHeader` |

#### 피드 광고

현재 `src/components/memories/memoryFeedAds.ts:4`에서 `EXAMPLE_FEED_ADS_ENABLED = false`이므로 **사용자에게 노출되지 않습니다.** 활성화 시 검토할 사항만 기록합니다.

| 심각도 | 위치 | 문제 |
|---|---|---|
| Low | `memoryFeedAds.ts:5, 42-64` | 3개마다 삽입 + 짧은 피드에 후행 광고 — 라이브러리가 작으면 과빈도 |
| Low | `MemoriesScreen.tsx:220, 608-610` | 숨김이 메모리에만 저장되어 재시작 시 재노출 |
| Low | `MemoriesScreen.tsx:280` | "모든 순간" 필터에서만 노출 — 필터 전환 시 컨텍스트 초기화 |

고지 자체는 양호합니다: "광고 · 예시" 라벨(`MemoryFeedAdCard.tsx:31`), 숨김 버튼(`:34-41`), 설명 시트(`:75-78`).

### 2.4 내비게이션 · 설정 · 프로필 · 가족공유 · 알림

#### 내비게이션 모델

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `MainTabs.tsx:109-115, 369` | 상담 탭 미노출 (§1-4) | 탭 노출 또는 상시 진입점 |
| High | `MainTabs.tsx:274-279` vs `:295, :311, :328` | `convertBirth` 파라미터 잔존 (§1-5) | merge 정책 통일 |
| Medium | `App.tsx:228-250` | 딥링크가 탭 라우트만 지원. `BabyProfile`·`FamilyShare`·`NotificationCenter` 경로 없음 | 링킹 확장 또는 문서화 |
| Medium | `App.tsx:246-247`, `MainTabs.tsx:54-56` | `knanny://mic` / `Mic` 라우트가 빈 크림색 화면(`MicPlaceholder`)을 렌더 | 오버레이 트리거로 리다이렉트 |
| Medium | `App.tsx:344-356` | 사용 중 초대 딥링크가 `MainNavigator`를 언마운트하고 온보딩으로 전환 — 앱을 벗어난 느낌, 취소 경로 없음 | 스택 화면에서 처리 |
| Medium | `MenuScreen.tsx:267-270` vs `SettingsHomeScreen.tsx:14` | 같은 설정 영역이 push와 Modal 두 메타포 혼용 | 하나로 통일 |
| Low | `BabyProfileScreen.tsx:100-105, 297` | 아기 생성 후 `navigate("MainTabs")`만 호출해 스택에 `mode: "create"`가 남을 수 있음 | `replace` |
| Low | `NavigationHeader.tsx:36-48` | `onBack` 없을 때도 64px 빈 슬롯 렌더 | 조건부 렌더 |

#### 다중 아기 전환

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| Medium | `MemoriesScreen.tsx:470-490` | 추억 탭에 아기 스위처나 활성 아기 표시가 없음 — **업로드 대상 아기가 불명확** | "올리는 아기: {이름}" 표시 |
| Medium | `BabySwitcher.tsx:59` vs `RecordHomeHeader.tsx:97` | 기본 트리거는 아기 이름, `switchButton` 변형은 "아기 바꾸기"만 표시 | 항상 활성 아기 이름 노출 |
| Low | `SettingsHomeScreen.tsx:11`, `AppSettingsModal.tsx:459-462` | "현재 선택된 아기" 기준으로 동작하는데 설정 화면에 그 아기가 표시되지 않음 | 헤더에 활성 아기 표시 |

#### 설정 · 프로필 정보 구조

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| High | `MenuScreen.tsx:176-181, 241-242` | "내 프로필"(`MyProfileScreen`)과 "계정 설정"(`AppSettingsModal` account)이 이름·언어·관계를 중복 관리. Darin ID는 한쪽에만 존재 | 프로필 허브 단일화, 계정 모달은 로그인/연동/내보내기 전용 |
| Medium | `AppSettingsModal.tsx:476-609` vs `:288-307` | 타이머·카테고리·단위는 즉시 반영, 계정 페이지는 명시적 저장 | 저장 모델 통일 |
| Medium | `MyProfileScreen.tsx:119-186` | 아바타만 즉시 저장, 나머지는 저장 버튼 — 한 화면에 두 모델 | 통일 또는 "사진은 즉시 저장" 명시 |
| Medium | `MenuScreen.tsx:194-200` vs `AppSettingsModal.tsx:612-618` | 일기 리마인더와 수유·수면 알림이 서로 다른 UI에 분산 | 알림 허브 단일화 |
| Low | `BabyProfileScreen.tsx:814-820` | 구성원 "비활성화"가 확인 없이 1탭 (삭제는 확인 있음, `:825-839`) | 확인 추가 |

#### 가족 공유 / 초대

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| Medium | `FamilyShareScreen.tsx:445-472` | 초대 코드에 만료 시각·역할 요약이 없어 유효 여부를 알 수 없음 | 만료·역할·유형 표시 |
| Medium | `FamilyShareScreen.tsx:367-385` | 역할 선택이 admin/editor뿐 — `FAMILY_ROLE_LABELS.viewer`(`types/family.ts:31`)가 존재하는데 미노출 | viewer 노출 또는 기본 권한 설명 |
| Medium | `RecordScreen.tsx:720-722`, `DiaryScreen.tsx:435-437` vs `MemoriesScreen.tsx:229, 479` | 기록·일기는 뷰어 배너가 있으나 추억은 업로드만 숨기고 설명 없음 | 배너 일관 적용 |
| Low | `FamilyShareScreen.tsx:197-203` | 복사 성공을 `Alert.alert`로 안내 — 흐름 차단 | 인라인 토스트 |
| Low | `SharedCaregiversRow.tsx:26` | 탭 가능한 행에 role/label 없음 | 접근성 속성 |

#### 알림

| 심각도 | 위치 | 사용자 경험 | 개선 방향 |
|---|---|---|---|
| Medium | `DiaryReminderSettingsModal.tsx:147-158, 187-191` | 토글 즉시 OS 권한 다이얼로그. 사전 설명 화면 없음 — 한 번 거부되면 회복 어려움 | priming 시트 후 요청 |
| Medium | `pushNotifications.ts:42-44` | 이미 허용된 경우에만 토큰 등록. 앱 내에서 권한을 요청할 명시 경로가 리마인더 토글뿐 | 알림 설정에 "알림 허용" 추가 |
| Medium | `PushToast.tsx:44-48` | `top: 14` 하드코딩, safe-area 미반영 — 노치/상태바에 겹칠 수 있음. `MainTabs.tsx:372`의 배너는 `insets.top + 8`을 쓰는 것과 대비됨. 4.2초 자동 소멸, 수동 닫기 없음 | `insets.top` 반영 + 닫기 |
| Medium | `NotificationCenterScreen.tsx:102-119` | 로딩 스켈레톤이 없어 조회 완료 전 "알림이 없어요"가 먼저 보임 | `LoadingState` |
| Medium | `NotificationCenterScreen.tsx:115-117` | 조회 실패 시 `setItems([])` — **오류와 "알림 없음"이 구분 불가** | `ErrorState` + 재시도 |
| Low | `NotificationBellButton.tsx:17-27` | 배지가 화면 포커스 시점에만 갱신 | 읽음 처리 후 즉시 갱신 |
| Low | `NotificationCenterScreen.tsx:204` | "알림 설정"이 설정 홈으로만 이동 | 해당 설정으로 딥링크 |

---

## 3. 횡단 이슈

### 3.1 국제화

| 심각도 | 위치 | 문제 |
|---|---|---|
| High | `src/i18n.ts` 전체 | 1,438줄 사전의 대부분이 **현재 앱과 무관한 이전 제품(돌보미 매칭 마켓플레이스: Emma, Care Proposal, 시범 세션, 신원 조회)** 의 잔재. 현재 앱에서 실제 사용되는 키는 `voice.*`, `tabs.*`, `aiChat.*` 정도 |
| High | `App.tsx:274-278, 363, 648` 외 대부분 화면 | 스택 타이틀·`Alert.alert` 본문·메뉴 행·가족공유·알림센터가 모두 하드코딩 한국어. `useLanguage`를 쓰는 파일은 13개뿐 |
| Medium | `src/types/profilePreferences.ts:12-16` + `:26-30` | English는 `disabled: true`("English · 준비 중")로 막았지만 **"기기 설정 따라가기"는 선택 가능**하고, `resolveAppLocale("system")`은 비한국어 기기에서 `en`을 반환. 이 경우 탭 라벨과 음성 오버레이만 영어로 바뀌고 나머지는 한국어로 남는 혼합 UI가 됨 |
| Medium | `src/components/LanguagePicker.tsx` | 파일 전체가 어디에서도 import되지 않는 **죽은 컴포넌트** |
| Low | `FamilyShareScreen.tsx:324-338` | 고정 등폭 탭에 `numberOfLines={1}` — 작은 화면에서 잘림 위험 |

### 3.2 디자인 토큰 준수

| 지표 | 수치 |
|---|---|
| `fontSize: <숫자>` 하드코딩 | 약 **784곳** |
| `fontSize: type.*` 토큰 사용 | **49곳** |
| `.tsx` 내 하드코딩 hex 색상 | 42개 파일 |
| 카드 radius 편차 | 16 / 18 / 22 혼재 |
| 시트 배경 투명도 편차 | 0.45 / 0.48 / 0.55 |

`theme.ts`에 `type`, `spacing`, `radius` 스케일이 정의되어 있으나 실질적으로 사용되지 않습니다. `12.5`, `13.5` 같은 반 픽셀 값이 섞여 화면 간 위계가 미묘하게 어긋납니다.

### 3.3 접근성 커버리지

| 지표 | 수치 |
|---|---|
| `accessibilityLabel` | 약 197곳 (48개 파일) |
| `accessibilityRole` | 약 180곳 (50개 파일) |
| `onPress` / `<Pressable` | 약 881곳 (72개 파일) |
| `maxFontSizeMultiplier` / `allowFontScaling` | 8개 파일 |
| `hitSlop` | 22개 파일 |

터치 요소 대비 접근성 속성 커버리지가 **대략 4분의 1** 수준입니다. `theme.ts:75-79`에 `fontScaleCap`이 정의되어 있으나 탭 바 등 일부에서만 쓰이고, 본문은 무제한 확대되어 고정 레이아웃을 넘칠 수 있습니다.

### 3.4 모션

`PressSlide.tsx:16-30`, `ConsultFab.tsx:24-27`, `TodayTimeline.tsx:144,171`은 `useReduceMotion`을 올바르게 존중합니다. 반면 그리드 타일 press scale(`OneTouchRecordGrid.tsx:390`), 자주 쓰는 기록 칩(`QuickRecordsBar.tsx:208`), 그리고 **모든 Modal의 `animationType="slide"`** 는 설정을 무시합니다.

### 3.5 오류 전달 방식

`Alert.alert`가 21개 파일에서 사용되며 `RecordDetailSheet`(16회), `BabyStickerVaultModal`(10회), `AppSettingsModal`(9회)에 집중되어 있습니다. 같은 앱 안에 인라인 `ErrorBanner`/`ErrorState` 패턴이 있는데도 복구 가능한 오류까지 블로킹 다이얼로그로 처리하는 경우가 많습니다. 반대로 `RecordHomeHeader.tsx:73`처럼 `.catch(() => undefined)`로 조용히 삼키는 경로도 있습니다.

`onRequestClose`는 확인된 27개 Modal 전부에 적용되어 있어 Android 뒤로가기 처리는 일관됩니다.

---

## 4. 잘 되어 있는 부분

| 영역 | 내용 |
|---|---|
| 파괴적 동작 | 계정 삭제의 "삭제" 타이핑 확인(`MenuScreen.tsx:305-361`), 로그아웃 확인, 타임라인 스와이프 삭제 확인 |
| 미디어 | 추억 업로드의 미저장 변경 보호(`MemoryUploadModal.tsx:117-123`)와 사진별 재시도(`:222-225`), 피드 재시도 배너 |
| 음성 | 명확한 상태 머신(listening/analyzing/result/error), `AccessibilityInfo.announceForAccessibility`(`BabyLogVoiceOverlay.tsx:251-264`), 파형의 장식 요소 숨김 |
| 접근성 | 카테고리 순서 변경의 증가/감소 대체 액션(`DraggableCategoryList.tsx:164-176`), 추억 카드의 상세 라벨(`MemoriesScreen.tsx:164-193`), 업로드 실패 배너의 `accessibilityRole="alert"` |
| 목록 | 일기의 `SectionList` + 스티키 헤더 + 성능 props, 추억의 pull-to-refresh |
| 광고 | "광고 · 예시" 명시, 숨김, 설명 시트 — 고지 원칙 준수. 현재 비활성 |
| 빈 상태 | 추억의 필터 인지형 빈 상태, 리포트의 카드별 빈 상태, 임신 리포트 게이트 |

---

## 5. 권장 처리 순서

### 1차 — 출시 전 (기능적 손실·오정보)

1. 마케팅 동의 저장 또는 체크박스 제거 (`App.tsx:616`)
2. 기록 삭제 확인 다이얼로그 (`RecordDetailSheet.tsx:1214`)
3. 마이크 비활성 사유 분기 + 시각적 잠금 표현 (`MainTabs.tsx:130-135`)
4. `convertBirth` 파라미터 잔존 수정 (`MainTabs.tsx:295, 311, 328`)
5. 일기 편집 미저장 변경 보호 (`DiaryComposeModal.tsx:286`)
6. 추억 업로드 완료 전 제출 차단 (`MemoryUploadModal.tsx:161`)
7. 알림센터 조회 실패와 빈 상태 구분 (`NotificationCenterScreen.tsx:115`)

### 2차 — 출시 직후 (접근성·가독성) — §0.6에서 대부분 적용

8. `faint` / `muted` / 주요 버튼 대비 토큰 조정 (`theme.ts`) — 적용 (§0.7)
9. `ActiveTimerSheet` / `AddCustomCategorySheet` / `GrowthRecordModal` 키보드 회피 — 적용
10. 필수 항목 미충족 사유 노출 — `ProfileSetupScreen` 적용. 약관 동의 화면은 미적용 (아래 참조)
11. 사진 대체 텍스트와 기록 생성 live region — 적용
12. 터치 타깃 44/48 일괄 상향 — `QuickRecordEditorSheet`만 적용, 전수 조사 남음

남은 2차 항목:

- 약관 동의 화면의 "약관 전문은 메뉴 › 개인정보/약관에서 확인할 수 있어요" 안내는 **존재하지 않는 경로**를 가리킵니다. 코드베이스 전체에 약관 화면도, 약관 URL도 없습니다. 스토어 심사에서 걸리는 항목이라 실제 약관 문서 링크가 먼저 필요합니다.
- 터치 타깃 전수 조사 (현재는 `TOUCH_MIN` 상수를 쓰는 화면과 안 쓰는 화면이 섞여 있음)

### 3차 — 다음 사이클 (구조)

13. 상담 탭 IA 결정 (노출 또는 상시 진입점)
14. 프로필/설정 정보 구조 단일화, 저장 모델 통일
15. 알림 허브 통합 + 권한 priming
16. i18n 정리 — 이전 제품 잔재 제거, "기기 설정 따라가기" 처리 결정, `LanguagePicker` 제거 또는 연결
17. 디자인 토큰 준수 (타이포 스케일, radius, 시트 스타일)
18. `ConsultScreen` 대화 가상화 + 스트리밍
19. reduce-motion 전면 적용

---

## 부록 A. 검증 방법

| 항목 | 방법 |
|---|---|
| 색 대비 | `theme.ts` 색상값에 대해 WCAG 2.x 상대 휘도 공식으로 직접 계산 |
| 토큰/접근성 커버리지 | 저장소 전역 패턴 계측 (`fontSize`, `accessibilityLabel`, `accessibilityRole`, `hitSlop`, `Alert.alert`, `<Modal`, `KeyboardAvoidingView`) |
| 화면별 이슈 | 4개 영역 병렬 정적 감사 후, High 항목은 원본 코드 재확인 |
| 참조 기준 | `.cursor/skills/ui-ux-pro-max` UX 가이드라인 및 react-native 스택 가이드라인 |

## 부록 B. 검증하지 못한 항목

| 항목 | 사유 |
|---|---|
| 실기기 Dynamic Type 최대 배율 레이아웃 | 실행 검증 필요 |
| iPhone SE 등 소형 기기에서 탭 바 중앙 버튼 `marginTop: -28` 클리핑 | 실기기 확인 필요 |
| Android 키보드 동작(`windowSoftInputMode`) | 실기기 확인 필요 |
| VoiceOver / TalkBack 실제 읽기 순서 | 실행 검증 필요 |
| 광고 노출 시 실제 피드 체감 | `EXAMPLE_FEED_ADS_ENABLED = false`로 비활성 |
| 실제 업로드 실패·부분 실패 시 화면 전이 | live Supabase 계정 필요 |
