/** Fallback transcript when STT server is unavailable. */
export const DEMO_VOICE_TRANSCRIPT =
  "Baby had a bowel movement around 9:42 AM. Color looked golden yellow. Also took a 40-minute nap after lunch.";
