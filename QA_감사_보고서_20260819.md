# 다린 앱 QA 감사 보고서

- 작성일: 2026-08-19
- 대상: 현재 워킹 트리 (`main` + 미커밋 변경분, `202608180001_eager_media_uploads.sql` 포함)
- 근거 문서: `검증` (Multi-AI QA 프롬프트 세트)
- 수행 범위: 코드 정적 감사. **DB 스키마 변경 없음.**

## 0. 후속 재검증 및 수정 결과 (2026-08-19)

### 0.1 이슈 분류

| 이슈 | 현재 코드 분류 | 결과 |
|---|---|---|
| C1 활성 아기 재시작 리셋 | **Already fixed** | 현재 사용자와 pointer 사용자가 같고, 접근 가능한 아기 목록에 pointer가 있을 때만 우선 복원. 아니면 기존 fallback 사용 |
| H2 임신 중 born 음성 기록 | **Already fixed** | 임신 스테이지에서 마이크 disabled, confirm/manual callback도 차단, 열린 overlay도 자동 종료 |
| H5 일기 취소 후 임시 사진 | **Already fixed + race 보완** | 저장 없이 닫으면 `discardSession`. 업로드 요청과 삭제가 경합하면 업로드 완료 후 한 번 더 삭제 |
| H6 원터치 기록 더블탭 | **Already fixed** | 동일 quick record의 1.2초 이내 반복 탭 무시 |
| H7 추억 부분 게시 실패 | **Already fixed** | media/tag 연결 실패 시 `memory_posts` 삭제. FK cascade로 연결된 media/tag 행도 제거 |
| H3 임신 중 born 리포트 | **Confirmed → Fixed** | 임신 중 기존 `EmptyState`로 출생 후 이용 안내만 표시 |
| H4 출생일 입력 초기화 | **Potential only** | `load` 의존성상 이론적 가능성은 있으나, 화면이 열린 상태에서 동일 아기의 `careSetup.child`를 갱신하는 실제 background 트리거는 재현하지 못함. 코드 변경 없음 |
| M1 quick records | **Confirmed (미수정)** | `darin:quick-records`는 계정/아기 공용. 스테이지 필터로 잘못된 항목 저장은 막지만 사용자 설정은 아기 간 공유 |
| M1 active timers | **Not reproducible / 이미 격리됨** | `{userId}:{babyId}` 스코프 키 사용 |
| M1 diary draft | **Not reproducible / 이미 격리됨** | `{userId}:{babyId}` 스코프 키와 legacy claim migration 사용 |
| M1 consult chat | **Confirmed (미수정)** | `darin:consult-chat` 기기 전역 키라 아기 A/B 간 대화가 공유됨 |
| M2 pregnancy diary label | **Confirmed (미수정)** | label이 snapshot이 아니라 현재 `due_date`로 계산돼 예정일 변경 시 과거 주수도 변경 |
| M8 온보딩 더블 제출 | **Confirmed → Fixed** | 완료 버튼에 ref 기반 동시 호출 가드와 disabled 상태 추가 |
| M9 온보딩 아바타 `file://` 저장 | **Confirmed → Fixed** | 로컬 URI를 `photo_url`에 쓰지 않고 생성 후 기존 `uploadBabyAvatar`로 Storage 업로드 |

### 0.2 변경 파일

- `App.tsx`: last-active baby 복원, 온보딩 아바타 후속 업로드
- `src/screens/MainTabs.tsx`: 임신 중 음성 기록 차단
- `src/screens/tabs/RecordScreen.tsx`: quick record 반복 탭 가드
- `src/components/babylog/DiaryComposeModal.tsx`: 취소 시 eager upload session 폐기
- `src/utils/eagerMediaUpload.ts`: 업로드 중 취소 경합 후 최종 객체 삭제
- `src/repositories/MemoriesRepository.ts`: eager memory 부분 실패 롤백
- `src/screens/tabs/BabyReportScreen.tsx`: 임신 중 출생 후 이용 안내 상태
- `src/screens/onboarding/OnboardingFlow.tsx`: 온보딩 완료 중복 제출 방지
- `src/repositories/BabyRepository.ts`: 로컬 아바타 URI의 DB 저장 차단
- `src/utils/childDisplay.ts`: DB의 raw string 상태값과 타입 정합성 보완

### 0.3 DB 변경

**DB migration 없음.**

### 0.4 자동 검증

| 테스트 | 결과 |
|---|---|
| `pnpm typecheck` | **PASS** |
| IDE lint (수정 파일) | **PASS** |
| `pnpm qa:mvp` | **PASS** |
| `pnpm qa:report` | **PASS** |
| `pnpm qa:build12` | **PASS** |
| pregnancy 전환/과거 일기 label 직접 로직 검사 | **PASS** — 예정일 변경 시 과거 label 변경도 확인 |
| live Supabase diary/memories/profile QA | **NOT TESTED** — disposable QA 계정 생성에 필요한 `SUPABASE_SECRET_KEY`가 없어 스크립트가 시작 전에 차단됨 |

### 0.5 요청된 End-to-End Regression

`PASS`는 자동 로직/정적 스모크로 해당 분기를 실행한 경우, `NOT TESTED`는 로그인된 실기기 조작이나 live Supabase write가 필요한 경우입니다.

| # | 테스트 | 결과 | 근거/제한 |
|---|---|---|---|
| 1 | 신규 born 사용자 | **NOT TESTED** | 실기기 신규 계정 필요 |
| 2 | 신규 pregnancy 사용자 | **NOT TESTED** | 실기기 신규 계정 필요 |
| 3 | pregnancy 기록 | **PASS** | 임신 카테고리/저장 타입 경로 typecheck 및 MVP smoke |
| 4 | pregnancy diary | **PASS** | 일기 모델·날짜·snapshot 로직 smoke |
| 5 | pregnancy에서 음성 기록 접근 | **PASS** | disabled UI + confirm/manual callback 이중 가드 확인 |
| 6 | early birth | **PASS** | `due_date`와 실제 `birth_date` 독립, 과거 birth date 허용 |
| 7 | late birth | **PASS** | 예정일 이후 실제 birth date 저장 분기 동일 |
| 8 | due date 경과 후 pregnancy 유지 | **PASS** | 상태 판별은 `child_status === 'unborn' && !birth_date`; due date로 자동 전환 없음 |
| 9 | pregnancy → born | **PASS** | `updateBabyProfile` 단일 선행 write가 birth date/status를 함께 저장 |
| 10 | 과거 pregnancy diary 유지 | **PASS** | 출생일 이전 일기는 gestational label 유지 |
| 11 | born 기록 | **PASS** | MVP care-log smoke |
| 12 | 아기 2명 전환 | **PASS** | `qa:build12` + 접근 가능한 baby 검증 |
| 13 | 앱 완전 종료 후 재실행 | **NOT TESTED** | pointer 복원 코드는 확인했으나 프로세스 종료/재실행 실조작 미수행 |
| 14 | logout/login | **NOT TESTED** | 다른 실제 계정 전환 미수행 |
| 15 | 원터치 기록 빠른 더블탭 | **PASS** | 동일 quick-record 1.2초 반복 입력 차단 |
| 16 | diary 사진 추가 후 취소 | **PASS** | session discard + 업로드 완료 후 race cleanup 확인 |
| 17 | diary 정상 게시 | **NOT TESTED** | live QA가 secret 부재로 시작 전 차단 |
| 18 | memory 사진 게시 | **NOT TESTED** | live QA가 secret 부재로 시작 전 차단 |
| 19 | network 실패 가능 범위 | **PASS** | eager memory 보상 delete, diary cancel race cleanup 정적 확인. 실제 packet loss는 미주입 |

> 아래 감사 본문은 최초 발견 당시의 원인과 재현 정보를 보존하며, 상태 판단은 이 후속 결과를 우선합니다.
- 표기 규칙
  - **[확인됨]** — 코드에서 직접 확인한 동작
  - **[추정]** — 코드 구조상 발생 가능하나 실행으로 재현하지 않음 (`검증` 문서의 *Potential risk*)

---

## 1. 현재 Flow 구조 (실제 코드 기준)

### 1.1 앱 진입 라우팅

```
SplashScreen
  └ resolvePostSplashPhase()            src/utils/appStartup.ts:13
      ├ 세션 없음 → terms → auth (AuthStartScreen)
      └ 세션 있음 → "postAuth"
            └ routeAuthenticatedSession()   App.tsx:483
                ├ isUserProfileComplete 실패 → ProfileSetupScreen
                ├ 유효한 초대코드 있음      → OnboardingFlow(invite)
                ├ isBabyProfileComplete 실패 → OnboardingFlow(babySetup)
                └ 그 외                    → restoreWorkspace() → MainTabs
```

로컬에 `careSetup`이 저장돼 있어도 서버 라우팅을 건너뛰지 않습니다 (`appStartup.ts:15` 주석 참조). 이 설계는 공유 기기 안전성 측면에서 올바릅니다.

### 1.2 아이 등록 경로 (3가지)

| 경로 | 진입점 | 서버 호출 | 이중 제출 가드 | 아바타 처리 |
|---|---|---|---|---|
| 온보딩 | `OnboardingFlow` step `born`→`baby` | `BabyRepository.ensureFromCareSetup` → RPC `create_baby_with_owner` | ref + disabled 가드 | 생성 후 `uploadBabyAvatar`로 Storage 업로드 |
| 아기 추가 | `BabySwitcher` → `BabyProfile{mode:"create"}` | `addBaby` → 동일 RPC (+선택적 프로필/아바타 후속 호출) | `saving` 플래그 | Storage 업로드 |
| 초대 참여 | `OnboardingFlow(invite)` | `accept_invite_code` RPC | 해당 없음 | 해당 없음 |

### 1.3 pregnancy / born 판별

핵심 술어는 두 곳뿐이며, 이 릴리스의 모든 분기가 여기에 의존합니다.

```32:34:src/utils/childDisplay.ts
export function isPregnancyStage(child: { childStatus: string; birthDate?: string }): boolean {
  return child.childStatus === "unborn" && !child.birthDate;
}
```

```92:100:src/utils/profileCompletion.ts
export function isBabyProfileComplete(baby: {
  name?: string | null;
  child_status?: string | null;
  birth_date?: string | null;
  due_date?: string | null;
} | null | undefined): boolean {
  if (!baby?.name?.trim()) return false;
  return baby.child_status === "unborn" ? Boolean(baby.due_date) : Boolean(baby.birth_date);
}
```

**[확인됨] 예정일 도달만으로 born이 되는 코드 경로는 존재하지 않습니다.** 앱 시작, 스플래시, `rehydrateFromServer`, 서버 동기화 어디에도 `due_date`를 보고 `child_status`를 바꾸는 로직이 없습니다. `검증` 문서의 핵심 규칙(`due_date 도달 ≠ born`)은 **충족**합니다.

전환은 오직 사용자 입력으로만 일어납니다.

```304:304:src/screens/BabyProfileScreen.tsx
      const becomingBorn = converting || Boolean(birthDate.trim() && careSetup.child.childStatus === "unborn");
```

### 1.4 스테이지 분리가 적용된 화면 / 적용되지 않은 화면

`isPregnancyStage` 호출처를 전수 조사한 결과입니다.

| 적용됨 | 미적용 |
|---|---|
| `RecordScreen` (원터치 그리드, 커스텀 카테고리, 자주 쓰는 기록) | `ConsultScreen` / `babyLogAIContext` |
| `MenuScreen`, `AppSettingsModal` | `MemoriesScreen`, `DiaryScreen` 요약 |
| `BabyProfileScreen`, `RecordHomeHeader`, `BabySwitcher` |  |
| `MainTabs` 음성 기록, `BabyReportScreen` 안내 상태 |  |

### 1.5 데이터 저장소

- 서버: Supabase Postgres + Storage (5개 비공개 버킷)
- 로컬: AsyncStorage. **일부만** `{userId}:{babyId}` 스코프가 적용돼 있습니다 (`src/utils/scopedLocalStorage.ts:18`).
  - 스코프 있음: `babyLogs`, `diary`, `familyMembers`, `customCategories`, `growthBookEdit`, `babyStickers`, `growthRecords`
  - **스코프 없음(기기 전역)**: `careSetup`, `quickRecords`, `activeTimers`, `consultChat`, `foodIngredients`, `diaryDraft`, `diaryReminder`

---

## 2. 데이터 모델 / DB

### 2.1 `babies` 테이블

```38:52:supabase/migrations/202607250001_care_logs_slice.sql
create table if not exists public.babies (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  birth_date date,
  due_date date,
  child_status text not null default 'newborn',
  ...
);
```

- `child_status`: `text NOT NULL DEFAULT 'newborn'` — **CHECK 제약도 enum도 없음**
- `birth_date`, `due_date`: 모두 nullable, **상호 정합성 제약 없음**
- 생성 RPC `create_baby_with_owner`는 `SECURITY DEFINER`이며 `auth.uid()` 검사 후 `baby_members`에 admin 행을 함께 삽입합니다. 상태/날짜 검증은 `coalesce(p_child_status, 'newborn')`이 전부입니다.
- **갱신용 RPC는 없습니다.** `BabyRepository.updateBaby`와 `BabyProfileRepository.updateBabyProfile`이 `babies` 테이블을 직접 UPDATE 합니다.

### 2.2 기존 사용자 호환성

**[확인됨] `child_status`나 `due_date`를 백필하는 마이그레이션은 존재하지 않습니다.** 컬럼은 최초 DDL부터 `NOT NULL DEFAULT 'newborn'`이므로, 임신 기능 추가 이전 데이터는 전부 `'newborn'`입니다. NULL이 될 수 없고, 기존 born 아이가 `'unborn'`으로 잘못 판정될 경로도 없습니다.

→ `검증` B 항목("기존 아이가 pregnancy로 잘못 판단되지 않는지")은 **통과**입니다.

### 2.3 RLS

전 테이블 RLS 활성, `using (true)` 정책 없음, 서비스 롤 키는 앱에 포함되지 않음(`src/lib/supabase.ts`). 스토리지 5개 버킷 모두 `public = false`이며, 최종 경로 읽기는 대응하는 DB 행(`diary_media` / `memory_media` / `growth_book_media` / `baby_stickers`) 존재와 열람 권한 헬퍼를 모두 요구합니다. 경로 추측만으로 타인 미디어를 읽을 수 없습니다.

임시 업로드 경로 `{baby_id}/temp/{session_id}/{file}`은 `can_edit_care_logs(baby_id)` (admin/editor)로 제한됩니다.

→ `검증` F/보안 항목은 **마이그레이션 파일 기준으로는 통과**입니다. 단, 운영 프로젝트의 실제 정책 상태는 저장소만으로 검증 불가.

---

## 3. 발견 이슈

### Critical

#### C1. 앱을 재시작할 때마다 활성 아기가 "가장 먼저 만든 아기"로 강제 리셋됨

- **문제**: 콜드 스타트 시 저장된 활성 아기 포인터(`darin:supabase-sync`)를 무시하고, `created_at` 오름차순 첫 번째 아기로 워크스페이스를 복원한 뒤 그 값으로 포인터를 **덮어씁니다**.
- **발생 조건**: 아기를 2명 이상 보유한 계정. 이번 릴리스의 대표 시나리오인 "기존에 태어난 첫째 + 새로 등록한 임신 둘째"에서 **항상** 발생합니다.
- **재현**
  1. born 아이가 등록된 계정으로 로그인
  2. 아기 추가 → "아직 뱃속에 있어요"로 둘째 등록 (자동으로 둘째가 활성화됨)
  3. 임신 기록을 몇 개 작성
  4. 앱 완전 종료 후 재실행
- **예상 결과**: 마지막에 보던 둘째(임신)가 활성 상태로 복원
- **실제 결과**: 첫째(born)로 되돌아감. 홈 헤더·기록 그리드·일기 전부 첫째 컨텍스트. 사용자가 알아채지 못하면 첫째에게 기록을 남기게 됨
- **원인**

```545:546:App.tsx
    const babies = await BabyRepository.listMyBabies();
    const preferredBaby = babies.find((baby) => baby.id === input?.preferredBabyId) ?? babies[0];
```

  콜드 스타트 경로(`App.tsx:588`)는 `routeAuthenticatedSession()`을 **인자 없이** 호출하므로 `preferredBabyId`가 `undefined`이고, `babies[0]`(가장 오래된 아기)이 선택됩니다. 이후 `restoreWorkspace` → `rehydrateFromServer({ babyId: serverBaby.id })`가 스코프를 override로 강제하고,

```416:425:src/context/BabyLogContext.tsx
    if (persisted) {
      if (sync.userId !== session.user.id || sync.babyId !== persisted.id) {
        await saveSupabaseSync({ userId: session.user.id, babyId: persisted.id, ... });
      }
      return { userId: session.user.id, babyId: persisted.id };
    }
```

  저장돼 있던 포인터까지 첫째로 갱신해버립니다. 즉 되돌릴 여지 없이 매번 리셋됩니다.
- **영향 범위**: 다자녀/임신 추가 사용자 전원. 기록·일기·성장북·스티커 등 아기 스코프 데이터 전부의 표시 대상이 바뀝니다.
- **수정 방향**: `routeAuthenticatedSession`에서 `preferredBabyId`가 없을 때 `getSupabaseSync()`의 `babyId`(단, `sync.userId === session.user.id`인 경우에만)를 우선 후보로 사용하고, 해당 아기를 찾지 못할 때만 `babies[0]`으로 폴백. `App.tsx` 1개 파일 수정으로 해결 가능.

---

### High

#### H1. 첫 번째 아기가 미완성이면 다른 아기가 정상이어도 앱 전체가 온보딩에 갇힘

- **원인**: `hasBaby` 판정이 `babies[0]` 단 하나만 봅니다.

```547:551:App.tsx
    const route = resolveAuthenticatedRoute({
      profileComplete,
      hasPendingInvite: Boolean(validPendingCode),
      hasBaby: Boolean(preferredBaby && isBabyProfileComplete(preferredBaby)),
    });
```

- **영향**: `babySetup`으로 진입한 뒤 온보딩을 마치면 `ensureFromCareSetup(setup, babies[0].id)`가 **첫째 아기 행을 로컬 setup으로 덮어씁니다**(`BabyRepository.ts:106`). 미완성 아기가 하필 첫째면, 정상적인 둘째는 손도 못 대고 첫째만 수정하게 됩니다.
- **발생 조건**: `child_status='newborn'`인데 `birth_date`가 없거나, `'unborn'`인데 `due_date`가 없는 행이 최초 생성 아기인 경우. UI 검증은 이를 막지만 **[추정]** 초대로 참여한 타인 생성 아기, 레거시/수동 삽입 행, 후속 호출 실패로 남은 행에서 가능합니다.
- **수정 방향**: `hasBaby`를 `babies.some(isBabyProfileComplete)`로 바꾸고, `preferredBaby`를 완성된 아기 중에서 고르기.

#### H2. 임신 중에도 음성 기록이 출산 후 카테고리로 저장됨

- **문제**: 중앙 마이크 탭(`MainTabs`)에는 스테이지 게이트가 전혀 없습니다. `voiceToBabyLog.ts`는 `formula`/`breast`/`diaper`/`sleep`/`memo` 등 born 전용 카테고리만 매핑합니다.
- **재현**: 임신 아이 활성 상태 → 마이크 탭 → "분유 120 먹였어" → 확인 → `formula` 로그가 임신 아이에 저장됨
- **결과**: `normalizeEntry`는 카테고리와 스테이지의 정합성을 검증하지 않으므로 그대로 저장·서버 동기화됩니다. 원터치 그리드는 임신 카테고리만 보여주는데 타임라인에는 born 카테고리 로그가 섞여 나타납니다.
- **영향**: `검증` "Record 공격 — category나 데이터가 섞이는지" 시나리오에 정면으로 해당.
- **수정 방향**: 임신 스테이지에서 마이크 버튼을 비활성화하거나(`allowAdd`와 동일 패턴), 음성 결과를 `memo`로만 축소.

#### H3. 임신 중 리포트 탭이 출산 후 지표를 그대로 노출

- `BabyReportScreen`에는 `isPregnancyStage` 호출이 없습니다. 임신 아이 활성 상태에서도 수유/수면/기저귀/성장 대시보드가 그대로 렌더됩니다.
- **[확인됨] 크래시나 음수 일령("생후 -N일")은 발생하지 않습니다.** `formatPostnatalAge`는 `Math.max(0, ...)`로 클램프하고(`childDisplay.ts:83`), `buildDashboardData`는 `birthDate` 부재 시 null을 반환합니다.
- 즉 데이터 손상은 없고, **전부 0인 빈 대시보드 + "생년월일을 입력해 주세요" 안내**가 나옵니다. 임신 사용자에게는 무의미한 화면입니다.
- **수정 방향**: 임신 스테이지에서 리포트 탭을 임신 요약(주수/체중/증상)으로 대체하거나 안내 상태로 치환.

#### H4. 출생 등록 화면에서 입력 중 값이 초기화될 수 있음

- **원인**: `load`가 `careSetup.child`를 의존성으로 갖고(`BabyProfileScreen.tsx:202`), `useFocusEffect(useCallback(() => { void load(); }, [load]))`로 묶여 있습니다. 화면이 포커스된 상태에서 `careSetup`이 갱신되면 `load`의 참조가 바뀌어 effect가 재실행됩니다.
- 재실행되면 `convertBirth` 파라미터가 아직 남아 있으므로 `applyBirthConvert`가 다시 동작하며:

```161:167:src/screens/BabyProfileScreen.tsx
      if (enterConvert && stillPregnant) {
        setConverting(true);
        setEditing(true);
        setBirthDate("");
```

  **사용자가 방금 고른 실제 출생일이 지워집니다.**
- **[추정]** `careSetup`을 갱신하는 주체는 `RecordHomeHeader`의 포커스 이펙트, `rehydrateFromServer`, 푸시 수신 등입니다. 실기기에서 타이밍 재현 필요.
- **수정 방향**: `load`의 의존성에서 `careSetup.child` 전체 객체를 빼고 필요한 원시값만 참조하거나, `convertBirth` 진입 처리를 최초 1회로 제한(`useRef` 가드).

#### H5. 일기 작성 취소 시 업로드된 임시 사진이 정리되지 않음

- `MemoryUploadModal`은 닫힐 때 `discardSession`으로 임시 객체를 삭제합니다(`MemoryUploadModal.tsx:105`). **`DiaryComposeModal`에는 동일한 정리 코드가 없습니다.**
- 사진을 고르는 즉시 `enqueuePickedPhotos`가 `{baby_id}/temp/...`로 선업로드하므로, 사진만 넣고 취소하면 스토리지에 고아 파일이 남습니다.
- 서버 정리 함수 `cleanup_orphan_temp_media()`는 **24시간 경과분만** 삭제하고, 호출 지점은 `MemoriesScreen` 피드 로드 한 곳뿐입니다(`MemoriesScreen.tsx:297`). 추억 탭에 들어가지 않는 사용자는 영원히 정리되지 않습니다. **[확인됨] pg_cron 등 서버 스케줄은 저장소에 없습니다.**
- **수정 방향**: `DiaryComposeModal`에도 `discardSession` 이펙트 추가 + 세션 ID를 모달 재오픈 시 재생성. 추가로 앱 시작 시 1회 `cleanup_orphan_temp_media` 호출.

#### H6. 원터치 기록 더블탭 시 중복 기록 생성

- `CareLogRepository.upsert`는 `(baby_id, client_generated_id)` 충돌로 멱등하지만, 원터치 그리드는 **탭할 때마다 새 `createId()`를 만들고 in-flight 가드가 없습니다**(`RecordScreen.tsx:582`).
- 급한 부모가 두 번 누르면 서로 다른 ID의 로그 2건이 로컬·서버 모두에 생깁니다.
- **수정 방향**: 카테고리별 짧은 디바운스 또는 pending 상태 가드.

#### H7. 추억 게시 부분 실패 시 롤백 없음

- 구 경로 `createMemoryWithImages`는 실패 시 스토리지 파일과 게시물 행을 되돌립니다(`MemoriesRepository.ts:713`). 신규 경로 `publishEagerMemory`에는 보상 로직이 없어(`MemoriesRepository.ts:720`), `memory_posts`는 생성됐는데 일부 `memory_media` 연결이 실패하면 **사진이 빠진 게시물**이 남습니다.
- 화면상으로는 낙관적 카드에 오류 배너가 뜨지만 서버에는 이미 반쪽 게시물이 있습니다.

---

### Medium

#### M1. 로컬 저장소 키 일부가 아기/계정 단위로 분리되지 않음

`src/utils/storageKeys.ts` 기준, 다음 키는 기기 전역입니다.

| 키 | 결과 |
|---|---|
| `darin:quick-records` | 아기·계정 간 "자주 쓰는 기록" 공유. 임신용 기본 항목이 born 아이에게도 저장됨(스테이지 필터로 표시는 걸러짐) |
| `darin:active-timers` | 아기 A의 진행 중 타이머가 아기 B 전환 후에도 보임 |
| `darin:consult-chat` | 정상 로그아웃을 거치지 않으면 이전 사용자의 상담 대화가 잔존 **[추정]** |
| `darin:care-setup` | 로그인 전 짧은 순간 이전 계정 아이 정보가 표시될 수 있음 **[추정]** |
| `darin:diary-draft`, `darin:food-ingredients`, `darin:diary-reminder` | 아기 간 혼재 |

`hydrateStorageState`에서 스코프를 넘기지 않는 호출이 근거입니다.

```486:490:src/context/BabyLogContext.tsx
      hydrateCustomCategories(scope, force),
      hydrateQuickRecords(force),
      hydrateBabyLogs(scope, force),
      hydrateDiaryEntries(scope, force),
      hydrateChatHistory(force),
```

#### M2. 일기 스테이지 라벨이 스냅샷이 아니라 렌더 시 계산

```104:113:src/utils/childDisplay.ts
export function formatDiaryStageLabel(
  child: Pick<ChildProfile, "birthDate" | "dueDate">,
  onDateKey: string,
): string | null {
  if (child.birthDate && onDateKey >= child.birthDate) {
    return formatPostnatalAge(child.birthDate, onDateKey);
  }
  if (child.dueDate) return formatGestationalAge(child.dueDate, onDateKey);
  return null;
}
```

- **좋은 소식**: `검증` E 항목의 핵심 요구("출산 후 과거 임신 일기가 생후 XX일로 바뀌면 안 됨")는 **충족**합니다. 일기 날짜가 출생일 이전이면 계속 임신 주수로 표시됩니다.
- **문제**: 라벨이 일기 행에 저장되지 않으므로, 병원에서 예정일이 변경돼 사용자가 `due_date`를 수정하면 **과거 임신 일기의 주수가 전부 소급 변경**됩니다. 참고로 `careLogSummarySnapshot`은 의도적으로 스냅샷 처리돼 있어(`DiaryScreen.tsx:355`) 라벨만 예외 상태입니다.
- 또한 `due_date`를 비우면 임신 시기 일기의 라벨이 사라집니다(`return null`).

#### M3. 출생 등록을 되돌릴 방법이 없음 (`검증` D-Case 6 부분 실패)

- 출생일 자체의 **수정**은 가능합니다(날짜 피커). 그러나 "실수로 출생 등록을 눌러 born이 된 아이"를 다시 임신 상태로 되돌리는 UI가 없습니다. `pregnancy`가 false가 되는 순간 예정일 입력 필드도 사라집니다(`BabyProfileScreen.tsx:601`).
- 날짜 피커가 값을 비울 수 없으므로 잘못된 상태로 고착되지는 않습니다(불행 중 다행). 사용자는 아기를 새로 만드는 것 외에 복구 수단이 없습니다.

#### M4. 예정일 경과 후 임신 주수가 무한 증가하고 출생 CTA가 영구 노출

```59:65:src/utils/childDisplay.ts
export function shouldShowBirthCta(child: ..., now = new Date()): boolean {
  if (!isPregnancyStage(child)) return false;
  const due = parseCalendarDate(child.dueDate);
  if (!due) return false;
  const remainingDays = calendarDayDifference(startOfLocalDay(due), startOfLocalDay(now));
  return remainingDays <= BIRTH_CTA_WINDOW_DAYS;
}
```

- 예정일 7일 전부터 노출되고, 예정일이 지나도 `remainingDays`가 음수라 조건이 계속 참입니다. 주석대로 "전환 전까지 계속 노출"이 의도라면 정상이나, "일주일만 노출" 요구와는 다릅니다.
- 함께 `formatGestationalAge`에 상한이 없어 예정일 3개월 경과 시 "임신 중 52주 3일" 같은 문구가 나옵니다. 예정일 피커의 최소값이 작년 1월 1일이므로 최대 ~125주까지 가능합니다.
- 다만 `검증` D-Case 4("예정일이 지났지만 출산 미등록")의 핵심 요구인 **pregnancy 상태 유지는 정상 동작**합니다.

#### M5. DB가 의미상 잘못된 상태를 그대로 허용

| 조합 | DB | 클라이언트 |
|---|---|---|
| `child_status='unborn'` + `birth_date` 존재 | 허용 | `isPregnancyStage`가 false → born으로 취급 |
| `child_status='newborn'` + `birth_date` NULL | 허용 | `isBabyProfileComplete` false → 온보딩으로 추방(H1과 연결) |
| 임의 문자열 `child_status` | 허용 | 읽을 때 `'newborn'`으로 정규화(`App.tsx:110`)되지만 DB 값은 그대로 |
| 과거/미래 극단 `due_date` | 허용 | 피커가 범위를 제한하지만 `updateBaby`에는 검증 없음 |

`BabyRepository.updateBaby`는 검증이 전혀 없는 직접 UPDATE입니다.

#### M6. 소프트 삭제 시 스토리지 파일이 남음

`soft_delete_diary_entry` / `soft_delete_memory_post` / `soft_delete_growth_book`은 `deleted_at`만 설정하고 미디어 행과 스토리지 객체를 남깁니다. `DiaryRepository.softDelete`, `MemoriesRepository.softDeleteMemoryPost`도 RPC만 호출합니다. 되돌리기를 위한 설계로 보이나, 복원 UI가 없다면 순수 누적입니다.

#### M7. `babies` 직접 INSERT 정책이 멤버십 없는 행을 허용

```251:254:supabase/migrations/202607250001_care_logs_slice.sql
create policy babies_insert_authenticated on public.babies
  for insert to authenticated
  with check (created_by = auth.uid());
```

의도된 경로는 `create_baby_with_owner` RPC이지만, 이 정책 때문에 `baby_members` 행 없이 아기를 삽입할 수 있습니다. SELECT는 `is_baby_member`를 요구하므로 **본인도 조회할 수 없는 고아 행**이 생깁니다.

#### M8. 온보딩 "시작하기" 버튼에 이중 제출 가드가 없음

`OnboardingFlow`의 완료 버튼은 `saving`/`disabled` 상태를 갖지 않습니다. 첫 아기 생성 직전에 두 번 누르면 두 `ensureFromCareSetup` 호출이 모두 `listMyBabies()` 결과가 빈 상태를 보고 각각 `createBaby`를 실행할 수 있습니다. **[추정]** — 네트워크가 느릴수록 창이 넓어집니다.

#### M9. 온보딩에서 고른 아기 사진이 로컬 URI로 저장됨

온보딩 경로는 `uploadBabyAvatar`를 호출하지 않고 `setup.child.photoUri`(예: `file://...`)를 RPC의 `p_photo_url`로 넘깁니다(`BabyRepository.ts:134`). 다른 기기·다른 가족 구성원에게는 깨진 이미지가 됩니다. "아기 추가" 경로는 정상적으로 Storage에 업로드합니다.

#### M10. `dayStart` 04:00 설정 시 날짜 이동 기준 불일치

`formatDateKey`는 `dayStart` 설정을 반영하지만(`dateKey.ts:9`), `offsetDateKey`는 항상 `"midnight"`으로 계산합니다. 새벽 0~4시에 캘린더 이동을 하면 "오늘" 라벨과 실제 이동 날짜가 하루 어긋날 수 있습니다.

#### M11. 타임아웃이 없는 네트워크 호출

`fetchWithTimeout`은 기본 30초, 음성 전사 90초로 설정돼 있으나, `openaiChat.ts`의 raw `fetch`와 **모든 Supabase 클라이언트 호출**에는 타임아웃이 없습니다. 불안정한 네트워크에서 저장 버튼이 무기한 스피너에 머물 수 있습니다.

---

### Low

- **L1**: `updateBabyProfile`이 오늘 +280일까지의 생년월일을 허용합니다(`BabyProfileRepository.ts:51`). UI 피커가 오늘까지로 제한하므로 앱에서는 도달 불가.
- **L2**: 예정일 피커 최소값이 작년 1월 1일이라 이미 한참 지난 예정일 입력이 가능합니다.
- **L3**: 임신 중 상담(Consult) 컨텍스트(`babyLogAIContext.FOCUS_CATS`)에 임신 카테고리가 없어 AI가 임신 기록을 참조하지 못합니다.
- **L4**: 설정의 카테고리 순서 편집이 임신 중에는 반영되지 않습니다. 임신 그리드는 고정 목록 `PREGNANCY_QUICK_ACTION_IDS`를 씁니다.
- **L5**: 출산 후 과거 임신 날짜를 조회하면 타임라인에는 임신 기록이 정상 표시되지만, 상단 요약 카드는 born 지표(수유/수면/기저귀)로 바뀌어 해당 날짜에 대해 전부 0을 보여줍니다.
- **L6**: `stage` 필드 도입 이전에 만든 커스텀 카테고리는 `"born"`으로 간주됩니다(`customCategoriesStore.ts:52`). 임신 중에 만들었던 것이라도 임신 UI에서는 숨겨집니다. 해당 카테고리를 쓰는 과거 로그는 타임라인에서 정상 렌더됩니다.
- **L7**: 임시 업로드 동시성 3, 개별 상한 25MB이므로 최악의 경우 순간 메모리 사용이 큽니다. 압축(장변 1800px, JPEG 0.8) 후에는 통상 문제되지 않습니다. **[추정]**

---

## 4. 전체 Flow 평가

| 항목 | 평가 | 근거 |
|---|---|---|
| 신규 born user | **PASS** | 이름+생년월일 필수 검증, RPC 저장, 재접속 유지 확인 |
| 신규 pregnancy user | **PASS** | 태명+예정일 필수, care 단계 스킵, `child_status='unborn'` 저장 |
| pregnancy recording | **PASS** | 임신용 그리드·자주 쓰는 기록·커스텀 카테고리 분리, born 음성 기록 차단 |
| pregnancy diary | **PASS** | 작성/조회/수정/삭제 정상, `client_generated_id` 멱등, 임신 주수 라벨 표시 |
| pregnancy → birth | **PASS (정적)** | 조기/지연 출생 모두 실제 출생일 저장. H4는 실제 트리거를 재현하지 못해 Potential only |
| early birth | **PASS** | `birth_date`와 `due_date`가 독립 저장되며 예정일보다 이른 날짜 허용 |
| late birth | **PARTIAL** | 전환은 정상. 예정일 경과 중 주수 무한 증가(M4) |
| existing user compatibility | **PASS (정적)** | 백필 없음·상태 오판정 없음, last-active pointer 복원 |
| diary continuity | **PARTIAL** | 출산 후에도 임신 라벨 유지(요구 충족). 예정일 수정 시 소급 변경(M2) |
| data persistence | **PARTIAL** | 서버 데이터는 안전. 로컬 일부 키 미분리(M1) |
| Supabase security | **PASS** | RLS 전면 활성, 비공개 버킷, 경로 추측 차단. *운영 프로젝트 실측은 미수행* |
| network failure | **PARTIAL** | 추억 부분 게시 롤백 보완. Supabase raw call 타임아웃 부재(M11)는 남음 |
| app reload | **PASS (정적)** | 현재 사용자 소유의 접근 가능한 last-active baby를 우선 복원 |

---

## 5. 출시 전 체크리스트

각 항목은 실기기에서 `PASS / FAIL / NOT TESTED`로 표기하세요. 아래 "정적 감사" 열은 이번 코드 리뷰 결과입니다.

| # | Flow | 정적 감사 | 실기기 |
|---|---|---|---|
| 1 | 회원가입 (이메일 / OAuth) | PASS | |
| 2 | 로그인 · 세션 복원 | PASS | |
| 3 | 기존 사용자 재로그인 (아기 1명) | PASS | |
| 4 | born child 온보딩 | PASS | |
| 5 | pregnancy child 온보딩 | PASS | |
| 6 | pregnancy Home (주수·D-day 표시) | PASS | |
| 7 | pregnancy record 작성 | PASS | NOT TESTED |
| 8 | pregnancy diary 작성·조회 | PASS | |
| 9 | photo upload (일기 / 추억) | PASS (코드·RLS 정적) | NOT TESTED |
| 10 | due_date 수정 | PARTIAL (M2) | |
| 11 | early birth (예정일 3주 전) | PASS | |
| 12 | late birth (예정일 5일 후) | PASS | |
| 13 | due date 경과 · 출생 미등록 | PARTIAL (M4) | |
| 14 | pregnancy → born 전환 | PASS (정적) | NOT TESTED |
| 15 | 전환 후 과거 임신 일기 확인 | PASS | |
| 16 | born records 작성 | PASS | |
| 17 | app reload 후 상태 유지 | PASS (pointer 검증) | NOT TESTED |
| 18 | logout → login | PARTIAL (M1) | |
| 19 | network failure 중 저장 | PARTIAL (M11) | |
| 20 | duplicate action (더블탭) | PASS (H6, M8 가드) | NOT TESTED |
| 21 | data ownership / RLS | PASS | |
| 22 | 아기 2명 전환 | PASS (`qa:build12`) | NOT TESTED |
| 23 | 계정 삭제 | PARTIAL (M6) | |

---

## 6. 출시 판단

### **READY WITH KNOWN ISSUES**

### 핵심 근거 3가지

1. **출시 blocker C1/H2는 제거됐습니다.** 다자녀 재시작은 현재 계정의 접근 가능한 last-active baby를 복원하고, 임신 아이에서는 born 음성 기록 진입과 저장 callback이 모두 차단됩니다.

2. **주요 P1 데이터 정합성 문제를 보완했습니다.** quick record 중복 생성, 일기 취소 temp object, 추억 부분 게시물, 온보딩 중복 생성과 로컬 아바타 URI 저장을 최소 범위로 막았습니다.

3. **실기기·live Supabase 최종 E2E는 남아 있습니다.** 정적 타입/린트와 로직 스모크는 통과했지만, disposable QA 계정용 secret이 없어 live diary/memories/profile 스크립트는 실행 전 차단됐습니다.

> 현재 알려진 잔여 위험은 M1의 전역 quick-record/consult-chat 설정, M2의 예정일 변경 시 과거 일기 주수 재계산, M11의 Supabase 호출 timeout 부재입니다. 이번 요청의 제외 범위에 해당해 수정하지 않았습니다.

---

## 7. 검증하지 못한 항목 (Potential risk)

이번 감사는 정적 코드 리뷰만 수행했습니다. 다음은 실기기·실서버 확인이 필요합니다.

- 운영 Supabase 프로젝트의 실제 RLS 활성 여부와 정책 드리프트 (저장소 마이그레이션과 일치하는지)
- `cleanup_orphan_temp_media`의 서버 스케줄 존재 여부 (저장소에는 pg_cron 설정이 없음)
- H4(입력값 초기화)의 실제 background 갱신 타이밍 재현
- 기기 타임존을 변경했을 때의 기존 로그 날짜 이동
- 대용량 사진 다중 업로드 시 실제 메모리 사용량
- 계정 삭제 엣지 함수(`delete-account`)의 공유 아기 스토리지 처리 결과
